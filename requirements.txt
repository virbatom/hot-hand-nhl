# Python 3.10 or newer required (the scraper uses `X | None` type hints).
#
# Install with:
#     pip install -r requirements.txt

polars
pandas
numpy
scipy
statsmodels
matplotlib
pyarrow          # parquet engine used by polars/pandas
requests         # used by the scraper
scikit-learn     # used by the scraper's expected-goals model
